/* All the Fizmo Glk references include "glkstart.h", because that's how I wrote the Unix Glk libraries such as GlkTerm.
 
	However, the IosGlk library uses "iosglk_startup.h". Therefore, we have this dumb little file which is named "glkstart.h" and includes "iosglk_startup.h".
 
	The end.
 */

#include "iosglk_startup.h"
